//
//  demo.swift
//  GoSnow
//
//  Created by federico Liu on 2024/8/6.
//
/*
import SwiftUI

struct LostItemCard: View {
    var body: some View {
        VStack(alignment: .leading, spacing: 10) {
            HStack {
                Image(systemName: "person.wave.2.fill") // 丢失的图标
                    .font(.title2)
                    .foregroundColor(.red) // 丢失用红色
                Text("丢失物品")
                    .font(.headline)
            }

            Divider()

            HStack {
                Text("物品名称：")
                    .bold()
                Text("（此处为物品名称）")
            }


            HStack {
                Text("联系方式：")
                    .bold()
                Text("（此处为联系方式）")
            }

            Text("备注：(此处为备注信息，例如物品特征描述等)")
                .font(.footnote)
                .foregroundColor(.gray)
                .padding(.top, 5)
        }
        .padding()
        .background(Color.white)
        .cornerRadius(10)
        .shadow(radius: 3) // 添加阴影
    }
}

#Preview {
    LostItemCard()
}
*/


